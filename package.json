{
  "name": "a4a-clan-bot",
  "version": "1.0.0",
  "description": "Discord bot for A4A Clan with One Piece theme",
  "main": "src/index.ts",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "db:push": "bunx prisma db push",
    "db:studio": "bunx prisma studio",
    "deploy": "bun run src/deploy-commands.ts"
  },
  "dependencies": {
    "@prisma/client": "latest",
    "discord.js": "^14.14.1",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "prisma": "latest",
    "typescript": "^5.3.3",
    "bun-types": "latest"
  }
}